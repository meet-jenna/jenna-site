# URL of the customer portal (meet-jenna dashboard app).
# Defaults to the deployed portal (https://portal.meetjenna.ai) when unset.
# For local development against a local portal, set this to http://localhost:3001.
NEXT_PUBLIC_PORTAL_URL=https://portal.meetjenna.ai
