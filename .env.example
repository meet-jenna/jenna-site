# URL of the customer portal (meet-jenna dashboard app).
# Defaults to the local dev server when unset.
# Set to the real host once the portal is deployed (e.g. https://portal.meetjenna.ai).
NEXT_PUBLIC_PORTAL_URL=http://localhost:3001
